#env
.env
.env.*

# Logs
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Generated standalone launchers (built from `dist/` via standalone/build.cjs) — see standalone/README.txt
standalone/Vaea-Windows.bat
standalone/Vaea-Linux.sh
standalone/_payload.*

# Generated native executables (built via standalone/exe/build.cjs) — see standalone/exe/README.txt
standalone/Vaea-Windows.exe
standalone/Vaea-Linux
standalone/exe/app

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.vite
base44/.app.jsonc
.app.jsonc

# Local app data (areas/products/projects/tasks/etc), written as plain JSON
# files by vite-localdb-plugin.js when running `npm run dev`/`npm run preview`
# from a cloned repo — see src/lib/localDb.js and README.md's "Local data
# storage" section. Never committed; this is per-clone user data, not source.
/data
